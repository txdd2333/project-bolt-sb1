# 服务提供商配置
# supabase: 使用 Supabase 数据库
# custom: 使用自定义 MySQL 数据库（推荐本地部署）
VITE_SERVICE_PROVIDER=custom

# API 服务器地址（前端访问后端的地址）
VITE_API_URL=http://localhost:3000

# MySQL 数据库配置（当 VITE_SERVICE_PROVIDER=custom 时使用）
# 标准 MySQL 示例：
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=your_password

# OceanBase MySQL 模式示例（推荐）：
DB_HOST=192.168.1.70
DB_PORT=2883
DB_USER=root@Tianji4_MySQL#Tianji4
DB_PASSWORD=aaAA11__
DB_DATABASE=ops_workflow_center

# 说明：
# - OceanBase 用户名格式: 用户名@租户名#集群名
# - 标准 MySQL 用户名格式: 用户名

# JWT 密钥（用于用户认证，请修改为随机字符串）
JWT_SECRET=change-this-to-a-random-secret-key-in-production

# 文件上传目录
UPLOAD_DIR=./uploads

# ========== 以下是 Supabase 配置（仅当 VITE_SERVICE_PROVIDER=supabase 时使用） ==========
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
