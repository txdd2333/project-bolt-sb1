# 双引擎快速上手

## 30 秒理解

```
一套代码 + 两个数据库引擎 = 随意切换

Supabase（云端）        MySQL/OceanBase（本地）
     ↓                         ↓
快速开发、多人协作        生产部署、内网隔离
```

---

## 5 分钟上手

### 当前配置

已经为您配置好了两个引擎：

```bash
✅ Supabase - 云端开发（已配置）
✅ OceanBase - 本地部署（已配置）
```

### 快速切换

#### Windows 用户

```powershell
# 切换到 Supabase（云端）
.\switch-to-supabase.bat
npm run dev

# 切换到 MySQL（本地）
.\switch-to-mysql.bat
npm run server  # 终端 1
npm run dev     # 终端 2
```

#### Linux/Mac 用户

```bash
# 切换到 Supabase（云端）
bash switch-to-supabase.sh
npm run dev

# 切换到 MySQL（本地）
bash switch-to-mysql.sh
npm run server &
npm run dev
```

#### 手动切换

```bash
# 编辑 .env 文件
# 修改第一行：
VITE_SERVICE_PROVIDER=supabase   # 使用 Supabase
# 或
VITE_SERVICE_PROVIDER=custom     # 使用 MySQL/OceanBase
```

---

## 使用场景

### 场景 1: 我在云端开发（当前）

```bash
# 1. 切换到 Supabase
bash switch-to-supabase.sh  # 或双击 .bat（Windows）

# 2. 启动开发
npm run dev

# 3. 访问
http://localhost:5173

# ✨ 优势：
# - 无需本地数据库
# - 5 分钟启动
# - 多人协作方便
```

### 场景 2: 我要本地测试

```bash
# 1. 切换到 MySQL
bash switch-to-mysql.sh

# 2. 启动服务（两个终端）
npm run server   # 终端 1: API 服务器
npm run dev      # 终端 2: 前端

# 3. 访问
http://localhost:5173

# ✨ 优势：
# - 连接真实生产数据库
# - 测试性能
# - 内网访问
```

### 场景 3: 我要对比测试

```bash
# 终端 1: Supabase（端口 5173）
VITE_SERVICE_PROVIDER=supabase npm run dev

# 终端 2: MySQL（端口 5174）
npm run server &
VITE_SERVICE_PROVIDER=custom npm run dev -- --port 5174

# 同时打开两个浏览器窗口对比
```

---

## 常见问题

### Q1: 切换引擎后数据会丢失吗？

A: **不会丢失！** 两个引擎使用**完全独立的数据库**。

```
Supabase 数据 → 存储在云端 PostgreSQL
MySQL 数据 → 存储在本地 MySQL/OceanBase

互不影响，切换只是选择连接哪个数据库
```

### Q2: 哪个引擎更快？

A:
- **Supabase**: 100-150ms（云端延迟）
- **MySQL**: 3-10ms（局域网）

**推荐**：开发用 Supabase（方便），生产用 MySQL（快）

### Q3: 我需要修改代码吗？

A: **不需要！** 完全透明切换。

```typescript
// 您的代码不需要任何改动
const { data } = await dataService.query('workflows')

// ServiceFactory 自动选择引擎
```

### Q4: 我能同时用两个吗？

A: 当前是单选模式，但可以实现数据同步（需要开发）。

---

## 配置文件说明

```
.env                  # 当前使用的配置
.env.example          # 配置模板
.env.supabase         # Supabase 预设配置
.env.mysql            # MySQL/OceanBase 预设配置

switch-to-supabase.*  # 切换脚本（Windows/Linux）
switch-to-mysql.*     # 切换脚本（Windows/Linux）
```

---

## 引擎对比卡片

```
┌─────────────────────┬───────────────┬──────────────────┐
│      特性           │   Supabase    │  MySQL/OceanBase │
├─────────────────────┼───────────────┼──────────────────┤
│ 启动时间            │ ⚡ 5 分钟     │ ⏱️ 15 分钟       │
│ 配置难度            │ ⭐ 简单       │ ⭐⭐⭐ 中等      │
│ 需要外网            │ ✅ 是         │ ❌ 否            │
│ API 延迟            │ 100ms         │ 5ms              │
│ 数据位置            │ 云端          │ 本地             │
│ 成本                │ 免费/付费     │ 硬件成本         │
│ 推荐场景            │ 开发/协作     │ 生产/内网        │
└─────────────────────┴───────────────┴──────────────────┘
```

---

## 下一步

### 立即试用

```bash
# 1. 试用 Supabase（推荐先试这个）
bash switch-to-supabase.sh
npm run dev

# 2. 注册账号、创建数据
# 在 http://localhost:5173 操作

# 3. 切换到 MySQL
bash switch-to-mysql.sh
npm run server
npm run dev

# 4. 查看数据（注意：数据是独立的）
```

### 深入了解

- 📖 [DUAL_ENGINE_GUIDE.md](DUAL_ENGINE_GUIDE.md) - 详细架构说明
- 📖 [WINDOWS_QUICKSTART.md](WINDOWS_QUICKSTART.md) - Windows 开发指南
- 📖 [START_HERE_CENTOS7.md](START_HERE_CENTOS7.md) - 生产部署指南

---

## 推荐工作流

```
第一周：Supabase 开发
├─ 快速搭建原型
├─ UI 开发和调试
└─ 功能验证

第二周：MySQL 测试
├─ 切换到本地数据库
├─ 性能测试
└─ 生产部署准备

持续：混合使用
├─ 新功能：Supabase 快速验证
└─ 稳定功能：MySQL 生产部署
```

---

**现在就试试吧！一行命令切换引擎！**

```bash
# Linux/Mac
bash switch-to-supabase.sh && npm run dev

# Windows PowerShell
.\switch-to-supabase.bat; npm run dev
```

更新时间：2024-01-18
