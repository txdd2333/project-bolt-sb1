# MySQL/OceanBase 本地开发配置
# 使用方式：cp .env.mysql .env

VITE_SERVICE_PROVIDER=custom

# API 服务器地址（前端访问后端）
VITE_API_URL=http://localhost:3000

# OceanBase 数据库配置（当前生产环境）
DB_HOST=192.168.1.70
DB_PORT=2883
DB_USER=root@Tianji4_MySQL#Tianji4
DB_PASSWORD=aaAA11__
DB_DATABASE=ops_workflow_center

# JWT 密钥（用于用户认证）
JWT_SECRET=change-this-to-a-random-secret-key-in-production

# 文件上传目录
UPLOAD_DIR=./uploads

# 说明：
# - 使用 MySQL/OceanBase 需要启动 API 服务器
# - 终端 1: npm run server
# - 终端 2: npm run dev
